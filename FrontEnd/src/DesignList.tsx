import type { OneItem } from "./types"

type DesignListProp = {
  data:OneItem[]
}

const DesignList:React.FC<DesignListProp> = ({data}) => {
  return (
    <div>
      
    </div>
  )
}

export default DesignList
